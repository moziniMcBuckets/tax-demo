[
  {
    "name": "sample_tool",
    "description": "A sample tool for testing AgentCore Gateway",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": {
          "type": "string",
          "description": "Input text to process"
        },
        "action_name": {
          "type": "string",
          "description": "Action name, must be 'sample_tool'"
        }
      },
      "required": ["action_name", "input"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "Processed result"
        }
      }
    }
  }
]
