{
  "name": "generate_upload_url",
  "description": "Generate secure presigned S3 URL for client document upload. Returns time-limited URL that clients can use to upload documents directly to S3.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "client_id": {
        "type": "string",
        "description": "Unique client identifier"
      },
      "filename": {
        "type": "string",
        "description": "Name of file to upload (must be PDF)"
      },
      "tax_year": {
        "type": "integer",
        "description": "Tax year (e.g., 2024)"
      },
      "document_type": {
        "type": "string",
        "description": "Type of document (W-2, 1099-INT, etc.)"
      }
    },
    "required": ["client_id", "filename", "tax_year", "document_type"]
  }
}
